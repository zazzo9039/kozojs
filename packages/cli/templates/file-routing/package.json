{
  "name": "{{PROJECT_NAME}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Kozo file-routing starter with JWT auth and admin guards",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/index.ts",
    "start": "tsx --env-file-if-exists=.env src/index.ts",
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@kozojs/auth": "^0.6.0",
    "@kozojs/core": "^0.6.0",
    "uWebSockets.js": "github:uNetworking/uWebSockets.js#v20.66.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0"
  }
}
