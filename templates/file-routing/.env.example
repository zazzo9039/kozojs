PORT=3000

# Required — the app will not start without it, and there is no default.
# Generate one and paste it below:
#   node -e "console.log(require('node:crypto').randomBytes(48).toString('base64url'))"
# Never commit the filled-in .env, and use a different value per environment.
JWT_SECRET=
